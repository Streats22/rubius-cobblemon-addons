# GUI textures (CobbleDollars-style, bundled for consistency)

**Current:** Only `shop_base.png` is drawn; the rest of the shop UI uses simple grey fills so the base texture is the single source of look. Other assets are for future use.

- **shop/** – Shop screen assets.
  - `shop_base.png` – **Only texture in use.** Main shop window background (252×196).
  - `category_background.png`, `category_outline.png` – For tabs (future).
  - `offer_background.png`, `offer_outline.png` – For list rows (future).
  - `buy_button.png`, `amount_arrow_*.png` – For right panel (future).
  - `bank_button.png`, `stock.png`, `category_*_short.png` – Reserved.

- **bank/** – `bank_base.png` – For bank / balance strip (future).
